<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    tools:context=".VacanciesFragment">

    <!-- Поле поиска -->
    <EditText
        android:id="@+id/edtv_search"
        android:layout_width="0dp"
        android:maxWidth="280dp"
        android:layout_height="40dp"
        android:layout_marginTop="48dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="8dp"
        android:background="@drawable/bg_rounded_edtv"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toStartOf="@id/imgbt_filter"
        app:layout_constraintStart_toStartOf="parent"/>

    <!-- Кнопка фильтра -->
    <ImageButton
        android:id="@+id/imgbt_filter"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginTop="48dp"
        android:layout_marginEnd="16dp"
        android:background="@drawable/bg_rounded_small_bt"
        android:src="@drawable/ic_filter"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/edtv_search"/>

    <!-- Число вакансий -->
    <TextView
        android:id="@+id/tv_vacancies_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_marginStart="16dp"
        android:text="0 вакансий"
        android:fontFamily="@font/sf_pro_display_regular"
        android:textColor="@color/white"
        android:textSize="14sp"
        app:layout_constraintTop_toBottomOf="@id/edtv_search"
        app:layout_constraintStart_toStartOf="parent"/>

    <!-- Режим сортировки -->
    <TextView
        android:id="@+id/tv_sorting_mode"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="6dp"
        android:text="По соответствию"
        android:fontFamily="@font/sf_pro_display_regular"
        android:textColor="@color/blue"
        android:textSize="14sp"
        app:layout_constraintTop_toBottomOf="@id/edtv_search"
        app:layout_constraintEnd_toStartOf="@id/img_sorting_mode"/>

    <!-- Иконка сортировки -->
    <ImageView
        android:id="@+id/img_sorting_mode"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_marginEnd="16dp"
        android:src="@drawable/ic_sorting"
        app:layout_constraintTop_toTopOf="@id/tv_sorting_mode"
        app:layout_constraintBottom_toBottomOf="@id/tv_sorting_mode"
        app:layout_constraintEnd_toEndOf="parent"/>


    <!-- Вертикальный RecyclerView для списка вакансий -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_vacancies"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="25dp"
        android:layout_marginHorizontal="16dp"
        app:layout_constraintTop_toBottomOf="@id/tv_vacancies_count"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"/>

</androidx.constraintlayout.widget.ConstraintLayout>